import { defineConfig } from '@tanstack/start';

export default defineConfig({
    // Puedes personalizar la configuración de TanStack Start aquí
    vite: {
        // Configuración personalizada para Vite
    },
});
