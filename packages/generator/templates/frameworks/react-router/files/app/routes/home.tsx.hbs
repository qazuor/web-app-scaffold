import { Welcome } from '../welcome/welcome.js';
import type { Route } from './+types/home.js';

export function meta({}: Route.MetaArgs) {
    return [{ title: '{{app.name}}' }, { name: 'description', content: '{{app.description}}' }];
}

export default function Home() {
    return <Welcome />;
}
