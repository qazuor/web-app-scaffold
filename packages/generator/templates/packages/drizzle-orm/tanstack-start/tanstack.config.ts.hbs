import { defineConfig } from '@tanstack/config';

export default defineConfig({
    database: {
        provider: '{{contextPackageVars.provider}}',
        url: process.env.DATABASE_URL || 'sqlite.db'
    }
});
